import { StyleSheet } from 'react-native';
import { Colors } from '../../theme';

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  blurView: {
    backgroundColor: Colors.appBgBlur
  }
});

export default styles;
